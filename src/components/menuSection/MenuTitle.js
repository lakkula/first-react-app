import React from 'react'

export default function MenuTitle({title}) {
  return (
    <div>
      <p style={{color: 'gray', textTransform: 'uppercase', fontSize:'12px'}}>{title}</p>
    </div>
  )
}
